1. Pull docker compose file.

```shell
curl https://raw.githubusercontent.com/bassamanator/nextcloud-postgres-how-to/main/compose.yaml --output compose.yaml
```

2. Start the nextcloud container.

```shell
docker compose up -d
```
